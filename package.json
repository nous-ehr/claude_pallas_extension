{
  "name": "pallas-claude-extension",
  "private": true,
  "scripts": {
    "build": "pnpm --filter @pallas/mcp-server build",
    "build:watch": "pnpm --filter @pallas/mcp-server build:watch",
    "typecheck": "pnpm -r typecheck",
    "clean": "pnpm -r clean",
    "start": "pnpm --filter @pallas/mcp-server start"
  },
  "devDependencies": {
    "typescript": "^5.4.5",
    "@types/node": "^22.0.0"
  }
}
